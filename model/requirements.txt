# Core ML training stack
unsloth[colab-new]>=2024.12
trl>=0.12.0
transformers>=4.47.0
peft>=0.14.0
accelerate>=1.2.0
bitsandbytes>=0.45.0

# Data processing
datasets>=3.2.0
tokenizers>=0.21.0

# Utilities
torch>=2.5.0
numpy>=2.2.0
tqdm>=4.67.0
rich>=13.9.0

# EVTX parsing (Windows event logs)
python-evtx>=0.7.4
lxml>=5.3.0

# Dataset generation / LLM labeling
openai>=1.58.0          # compatible with any OpenAI-format endpoint (Ollama, Gemini)
httpx>=0.28.0

# Evaluation
scikit-learn>=1.6.0
rouge-score>=0.1.2
